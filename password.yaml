# Password generator

matches:

  - regex: :pass\((?P<length>\d+)\)
    replace: "{{password}}"
    vars:
      - name: password
        type: shell
        params:
          cmd: bash "$(espanso path config)/match/public/scripts/password-generator.sh" "$ESPANSO_LENGTH" --print
